name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Check project model
        run: gradle projects

      - name: Run shared desktop tests
        run: gradle :shared:desktopTest

      - name: Build desktop classes
        run: gradle :desktop:compileKotlinJvm

      - name: Build Android debug APK
        run: gradle :android:assembleDebug
